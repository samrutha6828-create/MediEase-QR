{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "startCommand": "sh -c 'npx prisma db push --accept-data-loss && npx prisma db seed && npm start -- -p ${PORT:-3000}'",
    "restartPolicyType": "ON_FAILURE"
  }
}
